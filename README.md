Install the UV package manger 
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
uv --version  -- version 
In the Project :
   1 - uv init  -- (initalize the uv in the project)
   2 - uv venv -- (create a virtual Env.)
   3 - after is there is a comment to active the vitual env.
   4 - uv add -r requirement.txt (install the Package)
After creating a the respo in the Github 
-git remote add origin https://github.com/Soumik199/Langchain-Project.  -- get from the github only
-git branch -M main
-git push -u origin main / push through visual code 
